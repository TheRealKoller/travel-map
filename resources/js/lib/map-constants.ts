/**
 * Constants for map configuration
 */

/**
 * Default map center coordinates [latitude, longitude]
 * Default: Japan (latitude: 36.2048, longitude: 138.2529)
 * Note: This array uses [latitude, longitude] order, which will be swapped to [longitude, latitude] when used with Mapbox GL
 */
export const DEFAULT_MAP_CENTER: [number, number] = [36.2048, 138.2529];

/**
 * Default zoom level for the map
 */
export const DEFAULT_MAP_ZOOM = 6;
