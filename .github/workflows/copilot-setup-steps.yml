name: "Copilot Setup Steps"

# Automatically run the setup steps when they are changed to allow for easy validation, and
# allow manual testing through the repository's "Actions" tab
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/copilot-setup-steps.yml
  pull_request:
    paths:
      - .github/workflows/copilot-setup-steps.yml

jobs:
  copilot-setup-steps:
    name: Copilot Setup Environment
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.4
          tools: composer:v2

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install PHP Dependencies
        run: composer install --no-interaction --prefer-dist --optimize-autoloader

      - name: Install Node Dependencies
        run: npm ci

      - name: Setup Environment File
        run: cp .env.example .env

      - name: Generate Application Key
        run: php artisan key:generate

      - name: Run Database Migrations
        run: php artisan migrate --force

      - name: Build Frontend Assets
        run: npm run build
        env:
          VITE_MAPBOX_ACCESS_TOKEN: pk.eyJ1IjoidGhlLWtvbGxlciIsImEiOiJjbWs5eHBobTMwcTUwM2dyMmdwMGVqenQ4In0.dSooQl188W3T2_dRLUsZMg

      - name: Add ShadCN Tabs Component
        run: npx shadcn@latest add tabs

      - name: Add ShadCN Switch Component
        run: npx shadcn@latest add switch --yes

      - name: Add ShadCN Slider Component
        run: npx shadcn@latest add slider --overwrite
        
      - name: Verify Setup
        run: |
          echo "✅ Environment setup completed successfully"
          echo "PHP version: $(php -v | head -n 1)"
          echo "Node version: $(node -v)"
          echo "npm version: $(npm -v)"
          echo "Composer version: $(composer -V --no-ansi | head -n 1)"
